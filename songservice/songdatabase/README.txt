The future home of specific SongBase configuration and tooling.

Making the point by having this file that this database should not be shared by other services, even though in this current 
design it lends itself to that because its defined in the top level compose. Networking and VPNs will limit that.
